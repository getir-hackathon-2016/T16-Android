<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:orientation="horizontal">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/coke"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginLeft="@dimen/getir_spacing_small"
        android:orientation="vertical">

        <hackathon.polata.getir.view.GetirTextView
            android:id="@+id/list_item_cart_textview_name"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            tools:text="Atıştırma paketi"/>

        <hackathon.polata.getir.view.GetirTextView
            android:id="@+id/list_item_cart_textview_amount"
            style="@style/TextView.Getir"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="@dimen/getir_spacing_small_x"
            android:layout_weight="1"
            android:textColor="@color/colorPrimaryDark"
            tools:text="8.50 TL"/>
    </LinearLayout>
</LinearLayout>